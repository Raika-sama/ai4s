import React from 'react';
import { Box, Typography } from '@mui/material';

const RegisterTab = ({ classData }) => {
  return (
    <Box>
      <Typography variant="h6">
        Registro di Classe
      </Typography>
    </Box>
  );
};

export default RegisterTab;