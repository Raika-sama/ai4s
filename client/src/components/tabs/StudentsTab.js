import React from 'react';
import { Box, Typography } from '@mui/material';

const StudentsTab = ({ classData, setClassData }) => {
  return (
    <Box>
      <Typography variant="h6">
        Lista Studenti
      </Typography>
      {/* Implementeremo il contenuto più tardi */}
    </Box>
  );
};

export default StudentsTab;