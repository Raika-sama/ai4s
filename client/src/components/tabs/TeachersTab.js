import React from 'react';
import { Box, Typography } from '@mui/material';

const TeachersTab = ({ classData, setClassData }) => {
  return (
    <Box>
      <Typography variant="h6">
        Lista Docenti
      </Typography>
    </Box>
  );
};

export default TeachersTab;