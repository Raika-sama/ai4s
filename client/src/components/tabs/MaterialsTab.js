import React from 'react';
import { Box, Typography } from '@mui/material';

const MaterialsTab = ({ classData }) => {
  return (
    <Box>
      <Typography variant="h6">
        Materiali Didattici
      </Typography>
    </Box>
  );
};

export default MaterialsTab;